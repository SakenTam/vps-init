## 💡 如何使用

这是一个“下载并运行”的脚本。最简单的方法是使用下面的一键式命令。

### ⚡️ 一键安装 (推荐)

这种方法将下载、授权、运行合并为一条命令，但**必须在 `root` shell 下**执行。

**1. 切换到 Root 用户 (必须)**

```bash
sudo -i
```

**2. 复制并粘贴以下完整命令并执行**

```bash
curl -L -o init.sh https://raw.githubusercontent.com/SakenTam/vps-init/refs/heads/main/init.sh && chmod +x init.sh && bash ./init.sh
```

## 🧑‍💻 (备选) 手动分步执行

### 如果您想查看脚本内容或分步操作，这与上面的一键安装是等效的。

**1. 切换到 Root 用户 (必须)**

```Bash
sudo -i
```

**2. 下载脚本**

```bash
curl -L -o init.sh https://raw.githubusercontent.com/SakenTam/vps-init/refs/heads/main/init.sh
```

**3. 添加执行权限**

```bash
chmod +x init.sh
```

**4. 使用 bash 运行 (请勿使用 sh)**

```bash
bash ./init.sh
```
